import { ImojeInputDirective } from './imoje-input.directive';

describe('ImojeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ImojeInputDirective();
    expect(directive).toBeTruthy();
  });
});
